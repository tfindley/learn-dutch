services:
  learn-dutch:
    image: ghcr.io/tfindley/learn-dutch:latest
    container_name: learn-dutch
    ports:
      - "8080:80"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
